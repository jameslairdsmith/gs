# context("test_misc_functions")
#
# test_that("all_functions test works", {
#   expect_true(is_function_list(list(on_wday, on_mday, on_yday)))
#
#   expect_false(is_function_list(list(on_wday, on_mday, TRUE)))
# })
